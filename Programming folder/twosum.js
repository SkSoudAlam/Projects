function twosum(self, num, target){
    for (i in num, i < length(num), i++) {
        for (j in num, j < length(num), j++) {
            let result = i +j
            console.log(result)
        }
    }
}
